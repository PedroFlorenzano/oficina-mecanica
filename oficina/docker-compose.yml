services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: operare
      POSTGRES_PASSWORD: operare
      POSTGRES_DB: operare_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
